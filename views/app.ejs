<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ToDo App</title>
    <link
      rel="icon"
      href="https://images-platform.99static.com/npPkJtQ4LlzlkMFtNk40Ag-NQSk=/293x293:1719x1719/600x600/99designs-contests-attachments/126/126549/attachment_126549459"
      type="image/x-icon"
    />

    <link rel="stylesheet" type="text/css" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <h1 class="heading">TODO App</h1>
      <hr>
      <div class="addTask">
        <form action="/addTask" method="POST">
          <p>DESCRIPTION</p>
          <input
            type="text"
            name="description"
            placeholder="What do you need to do ?"
            required
          />
          <hr>
          <p>DUE DATE</p>
          <input type="date" name="dueDate" placeholder="dd/mm/yyyy" required />
          <label for="category">CATEGORY</label>
            <hr>
          <select name="category" id="category">
            <option>Choose a category</option>
            <option value="school">School</option>
            <option value="work">Work</option>
            <option value="personal">Personal</option>
          </select>
          <button class="btn">Add Task</button>
        </form>
      </div>
      <hr>
      <div class="taskList">
        <% if(Tasks.length==0) { %>
    
            <p>All the tasks are done..✌✌</p>
      
        <% } else { %>
        <% for(task of Tasks) { %>
            <p class="paragraph" ><%= task.description %></p>
            <p class="paragraph" ><%= task.dueDate %> </h3>
            <p class="category"><%= task.category %></p>
            <button> 
                <a href="">
                X
            </a>
            </button>
       <% } %>
       <% } %>
      </div>
    </div>

  </body>
</html>
